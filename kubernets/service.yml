apiVersion: v1
kind: Service

metadata:
  name: notes-app-service
  namespace: notes

spec:
  selector:
    app: django-notes-app

  ports:
    - protocol: TCP
      port: 8000
      targetPort: 8000



